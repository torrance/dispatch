<%= content_for(:title) { @page.title } %>

<div id="page" class="row">
  <div class="twelvecol">
    <h1 class="page-title"><%= @page.title %> <span class="edit-own-content"><%= link_to "(edit page)", edit_admin_page_path(@page) %></span></h1>

    <%= privileged_filter @page.body %>
  </div>
</div>