<%= content_for(:title) { "Editing “#{@content.title}”" } %>
<%= content_for(:head) { javascript_include_tag 'edit' } %>

<div class="row">
  <div class="twelvecol">
    <h1 class="page-title">Editing “<%= @content.title %>”</h1>
  </div>
</div>

<%= render 'form' %>