<%= content_for(:title) { "Create an #{@type}" } %>
<%= content_for(:head) { javascript_include_tag 'edit' } %>

<div class="row">
  <div class="twelvecol">
    <h1>Create <%= @type == 'repost' ? "a #{@type}" : "an #{@type}" %></h1>
  </div>
</div>

<%= render 'form' %>